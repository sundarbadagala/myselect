import { colors } from "./theme.colors";

export const defStyles = {
  color: colors.dark,
  backgroundColor: colors.lite,
  height: "40px",
};
export const slctStyles = {
  color: colors.dark,
  backgroundColor: colors.active,
};
export const hvrStyles = {
  color: colors.dark,
  backgroundColor: colors.hover,
};
